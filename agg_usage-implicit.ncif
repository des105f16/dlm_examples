principal a;

typedef struct t_usage {
  int {{a->a}} start_time;
  int {{a->a}} usageWh;
} t_usage;

int {{udata}} agg_usage(t_usage *udata, int {{_}} udata_size) {
  int sum = 0;
  int i = 0;

  while (i < udata_size) {
    sum = sum + udata[i].usageWh;
  }

  this -->? a {
    return <|sum|>;
  }
  return -1;
}
